import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Joke = ({ navigation }) => {
  return (
    <View>
      <Text>this is Joke</Text>
    </View>
  );
};

export default Joke;

const styles = StyleSheet.create({});
